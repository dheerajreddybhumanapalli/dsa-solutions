# Minimum Operations to Reduce X to Zero

Find the longest subarray with sum equal to `totalSum - x`, where `totalSum` is the sum of all elements in the array. The answer will be `n - lengthOfLongestSubarray`, where `n` is the length of the array. If no such subarray exists, return -1.

TC/SC: O(n)/O(1)
